{
    "version": 2,
    "routes": [
        {
            "src": "/server/(.*)",
            "dest": "/server/$1"
        },
        {
            "src": ".*",
            "dest": "/index.html"
        }
    ]
}